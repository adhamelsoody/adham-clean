# ذات القرآنية — تعريف المشروع

> **الصق هذا الملف في أول رسالة بأي جلسة جديدة، وارفع معه `app.js`.**

---

## ما هو

نظام إدارة حلقات قرآنية. يعمل على Firebase Hosting + Firestore + Auth.

| | |
|---|---|
| **المشروع في Firebase** | `mirath-72e2f` |
| **المجلد** | `C:\Users\People\Desktop\adham` |
| **بلا build step** | Firebase compat SDK، جافاسكربت خالصة |

---

## الملفات

| ملف | الحجم | ما فيه |
|---|---|---|
| `app.js` | **1486278** | كل المنطق — ١.٤ ميغا في ملف واحد |
| `styles.css` | **360427** | كل التنسيق |
| `admin/shell.js` | **35493** | قائمة الإدارة الجانبية |
| `firestore.rules` | **22644** | الصلاحيات — تُنشر بأمر منفصل |
| `sw.js` | **5644** | عامل الخدمة (العمل بلا إنترنت) |
| `quran.js` | **7459** | جداول المصحف |
| `manifest.json` | **1100** | ملف التطبيق (PWA) |

**ومجلد `quran/`** فيه ٦٠٧ ملفات بيانات المصحف — لا تُمسّ.

**وملفات ملحقة عندي وحدي:** `clean-boot.js` · `governance.js` · `duty-plus.js` · `manager-link.js` · `facilities-plus.js` — كلٌّ مربوط بـ١٦ صفحة.

---

## أوامر النشر

```powershell
cd C:\Users\People\Desktop\adham
node setup.js
firebase deploy --only hosting
firebase deploy --only firestore:rules     # عند تغيير القواعد فقط
```

ثم في المتصفح: **Ctrl+Shift+R**

**وعند تغيير `sw.js`:** F12 ← Application ← Service Workers ← Unregister

---

## الواجهات الخمس

| الدور | يرى |
|---|---|
| **مالك · مدير** | كل شيء |
| **مشرف** | مساجده وحدها · بلا مال ولا حسابات |
| **معلّم** | حلقاته وطلابه |
| **طالب** | نفسه |
| **ولي أمر** | أبناءه — وقد يكون له أكثر من ابن |
| **داعم** | لوحة الشرف وتقارير مسجده |

---

## الأنظمة المبنية

**الخطط والواجبات:** قوالب · اتجاه (تصاعدي/تنازلي) · سبع وحدات قياس · ارتباط شرطي · تعويض · انتقال بين المستويات

**الحضور:** تحضير يومي · حضور جزئي بالنسبة · مواقيت صلاة · إجازات تلقائية

**التسميع:** مصحف رقمي بعلامات ملوّنة · درجات · جلسات متعدّدة للواجب

**الإدارة:** مسيّرات رواتب · متجر جوائز باستحقاق · دورات وترحيل · قبول وتسجيل · سجل عمليات

**غيرها:** عمل بلا إنترنت · تطبيق PWA · وضع كبار السن · رسائل داخلية · إشعارات مفروزة بالدور

---

## طريقة العمل التي اتفقنا عليها

1. **افحص قبل أن تبني** — كثير مما يبدو ناقصاً مبنيٌّ بالفعل
2. **اختبر بعد كل تعديل** — بسكربت يشغّل الدوال فعلاً، لا بالنظر
3. **`node --check app.js`** بعد كل تعديل
4. **أرسل الملفات كاملة** مع **حجم كل ملف بالبايت** — يتحقّق منه بـ`dir`
5. **عربية فصيحة** في التعليقات والرسائل
6. **التعليق يشرح لماذا لا ماذا** — ما كان الخلل وكيف عولج

---

## أخطاء تكرّرت — احذرها

**١. دالّتان بالاسم نفسه** — الثانية تغلب الأولى بصمت. حدث مع `scopeOf`.

**٢. مرشّحان متوازيان** — `levelFilter` و`facilityFilter` كانا يحصران كلٌّ بطريقته، فتختفي البيانات.

**٣. `SHELL_MODE`** — يفعّله `admin/shell.js` في كل صفحة، فتُعامل شاشات المعلّم كملفّات HTML مستقلّة ولا تُفتح.

**٤. القراءة المباشرة من `DB`** بدل `cur()` — تتخطّى الحصر كلّه.

**٥. `it.count()` بلا حارس** — دالّة ترمي خطأً تُسقط رسم القائمة كلها.

---

## حالة معلومة

**حساب المدير يحمل `complexId` من مزامنة قديمة.** عولج في الكود (الحصر بـ`scopeIds` وحدها)، ويُنصح بمسحه من Firestore Console.

---

## ما لم يُبنَ — يحتاج قراراً

| البند | يحتاج |
|---|---|
| طيران الرسائل للجوال | خطة Blaze + بوّابة رسائل |
| تكامل زوم/ميت الكامل | حساب خدمة |
| قياس السطر بدقّة | بُني ثم رُجع — يُقارَب بالأوجه الآن |
| أيقونات التطبيق | مرسومة ومربوطة |

---

## المطابقة بالملف التقني: **٩٩٪**

٢٨ محوراً مكتملاً من ٢٩.
