# المستخدمون والحسابات — إكمال البنود الخمسة

## ⚠ اقرأ ده الأول

ثلاثة من الخمسة (الحذف الكامل · تعديل الدخول · الحسابات اليتيمة) **مستحيلة من المتصفح**. عميل Firebase مالوش صلاحية على حسابات غير حسابه — قرار أمني في فايربيس نفسه مش نقص في الكود.

يعني محتاج **Cloud Functions**، ودي تحتاج **خطة Blaze** (الدفع حسب الاستخدام).

**التكلفة الحقيقية:** الحد المجاني في Blaze ٢ مليون استدعاء شهرياً. نظامك هيستعمل عشرات في الشهر. عملياً صفر — بس لازم تربط بطاقة.

**اللي بيشتغل من غير Blaze:** شاشة الحسابات المعلّقة (البند ٣) فقط.

---

## الملفات

**جديدة:**

| الملف | مكانه | الدور |
|---|---|---|
| `accounts.js` | الجذر | شاشات الإدارة |
| `reset.html` | الجذر | استعادة كلمة المرور برمز الجوال |
| `functions/index.js` | مجلد `functions` | خمس دوال خادم |
| `functions/package.json` | مجلد `functions` | تبعيات الدوال |

**محدّثة (استبدل):** `setup.js` · `login.html` · `firebase.json` · `crud-plus.css`

---

## التركيب

```powershell
# ١) انسخ الملفات، ثم
node setup.js          # أو دبل كليك تركيب.bat

# ٢) ثبّت تبعيات الدوال
cd functions
npm install
cd ..

# ٣) انشر
firebase deploy --only functions
firebase deploy --only hosting
```

لو الأمر التاني قال `functions requires Blaze`، يبقى محتاج الترقية: Firebase Console ← ⚙ ← Usage and billing ← Modify plan.

---

## تفعيل الدخول بالجوال — مطلوب للبند ٢

Firebase Console ← **Authentication** ← **Sign-in method** ← فعّل **Phone**.

وفي **Settings ← Authorized domains** تأكد إن نطاق موقعك موجود.

**الحصة المجانية:** فايربيس بيدي عدداً محدوداً من رسائل SMS يومياً مجاناً، وبعدها بالتكلفة. راجع صفحة التسعير — تختلف حسب الدولة.

**شرط لا غنى عنه:** الرقم في ملف المستخدم لازم يكون مضبوطاً، لأن الخادم بيقارن الجوال المحقَّق من فايربيس بالرقم المسجَّل. لو الرقم غلط أو ناقص، الاستعادة هترفض — وده مقصود.

---

## البنود الخمسة

### ١. الحذف الكامل
زر «حذف نهائي» بينادي `deleteUserFully` فتمسح المستند **وحساب الدخول معاً**.

حماية مبنية في الخادم: **آخر مدير فعّال ما يتحذفش**، والمدير ما يقدرش يحذف نفسه. من غير دول ممكن يتقفل النظام على الكل.

### ٢. استعادة كلمة المرور بالجوال
`reset.html` بثلاث خطوات: الهوية والجوال ← الرمز ← كلمة المرور الجديدة.

**الأمان:** الرمز بيتبعت من فايربيس نفسه، والخادم بيقارن رقم الجوال **المحقَّق داخل رمز الجلسة** بالرقم المسجَّل في الملف. مفيش حد يقدر يدّعي رقم غيره. وبعد التغيير بتتلغى كل الجلسات القائمة، فاللي سرق كلمة المرور القديمة يخرج فوراً.

`login.html` اتعدّل: كان بيقول لحسابات رقم الهوية «راجع إدارة النظام» — دلوقتي بيوجّههم لـ `reset.html` ومعاهم رقم الهوية جاهز.

### ٣. الحسابات المعلّقة
لوحة في صفحة المستخدمين بتعرض كل من سجّل دخوله ولا ملف له، بعدّاد أحمر. زر «مراجعة» يفتح اعتماد بدور محدَّد أو رفض وحذف.

**دي الوحيدة اللي بتشتغل من غير Blaze** (العرض على الأقل — الاعتماد نفسه بينادي دالة).

### ٤. تعديل بيانات الدخول
`updateUserLogin` بتغيّر البريد ورقم الهوية في المستند **وفي Authentication معاً**.

**الحماية:** الخادم بيسمح للمدير أو **لصاحب الحساب نفسه** بس. والتحقق بيتعمل في الخادم مش في الواجهة — إخفاء الزرار مش حماية.

بيمنع كمان: بريد مستعمل في حساب تاني، ورقم هوية مكرر.

### ٥. الحسابات اليتيمة
زر «فحص الآن» بيقارن كل حسابات الدخول بكل الملفات ويعرض الاتجاهين:

- **حساب دخول بلا ملف** — يقدر يسجّل دخول وملوش دور. بيبان معاه آخر دخول، وزر حذف.
- **ملف بلا حساب دخول** — بيظهر في القوائم ومش قادر يدخل.

كل عملية من الخمسة بتتسجّل في **سجل العمليات** من الخادم مباشرة، فمينفعش حد يعمل حاجة ويخفيها.

---

## اختبار بعد النشر

1. **حساب معلّق:** سجّل بحساب جديد من `login.html` → لازم يقول «بانتظار الاعتماد». افتح صفحة المستخدمين بحساب المدير → لازم يظهر في اللوحة بعدّاد أحمر.
2. **الاستعادة:** افتح `reset.html`، اكتب هوية وجوال مسجّلين → لازم يوصل رمز.
3. **جوال غلط:** جرّب نفس الهوية برقم جوال تاني → **لازم يرفض**. لو نجح، فيه مشكلة أمنية وقوللي فوراً.
4. **الفحص:** اضغط «فحص الآن» في لوحة اليتيمة → لازم يعرض الأعداد.

---

## بند ما زال معلّقاً

`governance.js` بيضيف مصفوفة صلاحيات معلم **موازية** لمصفوفة `TEACHER_PERMS` الموجودة أصلاً في `app.js` بمفاتيح مختلفة (`attend_late` مقابل `attLate`). النظامان مش متصلين — اللي تعدّله في واحد ما بيأثرش في التاني.

ده خطأ مني: بحثت عنها بأمر مكتوب غلط فرجع صفر فافترضت إنها مش موجودة. لازم يتوحّدوا في مصفوفة واحدة قبل ما تعتمد على أي منهم.
