# ذات القرآنية — دليل العمل على المشروع

نظام إدارة حلقات قرآنية. عربيّ بالكامل، اتجاهه RTL.
**اقرأ هذا الملف أولاً. معظم ما تحتاج معرفته عن بنية المشروع هنا — لا تُعِد اكتشافه بالبحث.**

---

## ١. القواعد — غير قابلة للتفاوض

1. **نفّذ المطلوب فقط.** لا ميزة زائدة، ولا تحسين لم يُطلب، ولا إعادة هيكلة على الهامش.
2. **لا تلمس ملفاً خارج نطاق الطلب.**
3. **لا تُعِد التصميم ولا الكتابة** لأيّ جزء قائم إلا بطلبٍ صريح.
4. **لا تحذف كوداً ولا ميزةً ولا حقلاً** إلا بطلبٍ صريح. إن اضطرّك التعديل إلى إخراج حقلٍ من مكانه، **انقله** ولا تحذفه، وقل أين ذهب.
5. **لا تمسّ Firebase ولا Firestore ولا `firestore.rules` ولا `firebase.json` ولا `.firebaserc`** إلا بطلبٍ صريح.
6. **لا تغيّر التصميم القائم** إلا في الجزء المطلوب وحده.
7. **حافظ على RTL والعربية وهوية المشروع** في كلّ ما تكتب: نصوص الواجهة عربية، والتعليقات عربية، والأرقام العربية عبر `toArabicDigits` حيث يجري العُرف.
8. **حافظ على أسماء الدوال والحقول والواجهات** كما هي. تغيير اسمٍ قائم يكسر بياناتٍ محفوظة — لا تفعله إلا لضرورةٍ تشرحها.
9. **لا تُنشئ ملفاً جديداً** إن أمكن التنفيذ في ملفٍ قائم.
10. **لا بياناتٍ تجريبية في قاعدة البيانات الحقيقية**، ولا حذف لبياناتٍ فيها.
11. **لا نسخةً ثانية من المشروع** ولا من ملفاته.
12. **لا تفترض.** ما لم يُطلب لا يُنفَّذ.
13. إن كان الطلب غامضاً فعلاً: **سؤالٌ واحدٌ محدّد**، لا أكثر. وإن كان واضحاً: **ابدأ فوراً** بلا استئذان ولا عرضِ خطة.
14. إن عجزتَ عن بندٍ: **قف وقل السبب والملفَّ المسؤول.** لا تخترع التفافاً.

## ٢. أسلوب الردّ

- **الردّ النهائي مختصرٌ جداً.** بصيغة: ما تمّ · الملفات · الاختبار · ما تبقّى.
- **لا شرح مطوّل أثناء التنفيذ**، ولا سردٌ لكلّ خطوة.
- لا تسأل عمّا يمكن معرفته من المشروع نفسه.
- لا تقل «تمّ» قبل التحقّق، ولا «نُشر» قبل تأكيد نجاح النشر.

## ٣. قبل التعديل

1. حدّد الملفات المرتبطة بالمهمة من الخريطة أدناه.
2. اقرأ الكود القائم في موضعه — لا تعتمد على ذاكرة المحادثة.
3. نفّذ **أقلّ تعديلٍ ممكن** يحقّق الطلب.

## ٤. بعد التعديل

1. تحقّق من صحّة البناء: `node -e "new Function(require('fs').readFileSync('app.js','utf8'))"`.
2. تحقّق أنّ التعديل **موجودٌ فعلاً** في الملف (`git diff` أو `grep`).
3. تحقّق أنّ الميزات القديمة لم تتأثّر.

---

## ٥. خريطة المشروع

مشروع **بلا أداة بناء ولا إطار عمل**: HTML + CSS + JavaScript عاديّ يُحمَّل مباشرةً.

### الملفات

| الملف | الحجم | الدور |
|---|---|---|
| `app.js` | **١.٨ م.ب · ٣٥٢١٤ سطراً** | كلّ المنطق والشاشات. **الأثقل — اقرأ منه مقاطع بأرقام الأسطر، لا الملف كلّه** |
| `styles.css` | ٤٤٣ ك.ب · ٩٤٣٦ سطراً | كلّ التنسيقات. التعديلات الجديدة تُضاف في آخره |
| `auth.js` | ٤٦ ك.ب | إقلاع `index.html` |
| `admin/shell.js` | ٥٨ ك.ب | إقلاع صفحات `/admin/*.html` |
| `login.html` | — | صفحة الدخول الوحيدة (برقم الهوية + كلمة المرور) |
| `quran/` | ٢.٦ م.ب · ٦٠٤ ملف | جداول المصحف. **لا تبحث داخلها — استثنِها من أنماط البحث** |
| `sw.js` | — | عامل الخدمة: الشبكة أوّلاً للـ js/css/html |
| `functions/` | — | دوال Firebase |

### ثلاثة مسارات إقلاع منفصلة

| الصفحة | المُقلِع | ملاحظة |
|---|---|---|
| `index.html` | `auth.js` | |
| `/admin/*.html` | `admin/shell.js` عبر `window.ADMIN_PAGE` | |
| — | `init()` داخل `app.js` | يعمل فقط حين `__DEFER_INIT` غير مفعّل |

**`auth.js` و`shell.js` لا يستدعيان `init()` أبداً.** أيّ كودٍ تضعه داخل `init()` لن يعمل في الصفحات الفعليّة — ضعه في IIFE مستقلّة.

### مواضع مهمّة في `app.js`

| السطر | الموضع |
|---|---|
| ١٥ | `ICONS` — أيقونات SVG، و`ic(name,size)` في ٨٣ |
| ١٢٥ | `DB` — مرايا المجموعات في الذاكرة |
| ٢١٧ | `NAV` |
| ٣٣٧ / ٣٤٣ | `recByName` / `idByName` — الربط بالمعرّفات لا بالنصوص |
| ٣٤٩ → ٢١٦٢١ | شاشات الإدارة بالترتيب: المجمّعات · اللوحة · المساجد · الحلقات · **الطلاب (١٩٩٦)** · المعلّمون · البرامج · **الخطط (٢١٦٧)** · الحضور · التقارير · الإعدادات |
| ٤٢٦٣ / ٤٣٣٢ | `syncStudentRecord` / `syncTeacherRecord` |
| ٨١٣٩ | `PLAN_QTY_TYPE` — نوع الكمية في بطاقة الطالب وجدول الخطط |
| ٩٩٥٦ | `SYS_QTY_TYPES` — نوع الكمية في شاشة خطة البرنامج (**قائمةٌ أخرى بالاسم نفسه**) |
| ١٣٩٤١ / ١٤٠٢٢ | `nextHifzFrom` / `generateAssignments` — محرّك الخطة |
| ٢٦٠٥٣ / ٢٦٧١٨ | `PAGES` / `mount()` |
| ٢٩٥٩٩ | `panelStudent` — بطاقة الطالب |
| ٣٢٩٨٣ | `persistSet` — **بوابة الكتابة الوحيدة** |
| ٣٤٢٥٨ | `cur(coll)` — بوابة القراءة المصفّاة |

> الأرقام تتغيّر مع التعديل. استعملها للقفز التقريبي ثمّ تأكّد بـ `grep -n`.

### أعرافٌ يجب احترامها

- **كلّ كتابةٍ عبر `persistSet(coll, obj)`** — فيها `guardArchived` و`guardSession` والتدقيق وطابور العمل دون اتصال. لا تكتب إلى Firestore مباشرة.
- **كلّ قراءةٍ مصفّاةٍ عبر `cur(coll)`** لا من `DB.<coll>` مباشرة.
- بطاقة الطالب تحفظ مسوّدتها في `STU_DRAFT`؛ `panelStudent` يعرض `{...record, ...draft}`. **الحقل الغائب عن العرض لا يُكتب** فلا يُمحى بفراغ.
- التبويبان في بطاقة الطالب (`#spSecInfo` / `#spSecPlan`) **يُرسمان معاً مرّةً واحدة**، والتنقّل إظهارٌ وإخفاء لا إعادةَ رسم. لا تُعِد `panelStudent` عند تبديل التبويب.
- عند إضافة خيارٍ إلى قائمةٍ محفوظة: **أضِف ولا تحذف** الخيارات القديمة، وإلّا كُسرت السجلّات التي تستعملها. وإن كانت القيمة المحفوظة خارج القائمة فأضفها إليها عند العرض.
- التنسيقات الجديدة تُضاف في **آخر `styles.css`** مع تعليقٍ عربيّ يشرح السبب.

---

## ٦. البناء والنشر والاختبار

- **لا خطوة بناء.** التعديل في الملف يكفي.
- `version.json` يحمل رقم البناء `YYYYMMDD-HHMM` — **يُرفع مع كلّ نشر**.
- النشر: `firebase deploy --only hosting` (المشروع `mirath-72e2f`).
- **تحقّق من النشر بنفسك** بجلب `https://mirath-72e2f.web.app/version.json` ومطابقة الرقم. الدفع إلى git ليس نشراً — سقط النشر بعد دفعٍ ناجح أكثر من مرّة.
- في PowerShell: `$ErrorActionPreference = "Continue"` — `git push` يكتب في stderr عند نجاحه، و`Stop` يُجهض السكربت قبل النشر. ولا تستعمل `cmd /c` للالتزام.
- الاختبار: Playwright على نسخةٍ محليّة. مجموعات `t1..t11` تغطّي بطاقة الطالب والخطط والدخول.

## ٧. توفير الـ context — مهمّ

- **لا تقرأ `app.js` كاملاً** (≈٦٢٨ ألف رمز — ثلاثة أضعاف نافذة السياق). اقرأ بـ `offset`/`limit`، أو `grep -n` ثمّ اقرأ المقطع.
- **استثنِ `quran/` و`.git/` و`_backup_*` و`icons/`** من كلّ بحث.
- `grep` بـ `output_mode: "files_with_matches"` أوّلاً، و`content` عند الحاجة فقط مع `-n` و`head_limit`.
- **لا تُعِد فحصاً أجريتَه في الجلسة نفسها** بلا سبب. ولا تقرأ ملفاً عدّلتَه للتوّ للتأكّد — الأداة تُخطئك إن فشل التعديل.
- للتعديلات الصغيرة استعمل `Edit` لا إعادةَ كتابة الملف.
- مجلدات `_backup_*` تُنشئها سكربتات التحديث في الجذر: **تجاهلها تماماً**.
